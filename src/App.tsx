import { Button } from "./components/ui/Button"
import './App.css'

function App() {

  return(
    <>
      <Button variant="secondary" size="sm" onClick={()=>{}} text="Share"/>
      <Button variant="primary" size="md" onClick={()=>{}} text="Add Content"/>
      <Button variant="primary" size="lg" onClick={()=>{}} text="Add Content"/>
    </>
  )
}

export default App
